*.log
.DS_Store
.env
.env.backup
.env.production
.phpactor.json
.phpunit.result.cache
/.fleet
/.idea
/.nova
/.phpunit.cache
/.vscode
/.zed
/auth.json
/node_modules
/public/build
/public/hot
/public/storage
/storage/*.key
/storage/pail
/vendor
Homestead.json
Homestead.yaml
Thumbs.db

# Temporary local QA tooling (Playwright driver scripts + screenshots) —
# not part of the product, kept out of the repo. See qa-shots/ locally
# for the visual QA evidence from this submission's manual review pass.
.qa-*.cjs
/qa-shots

# Playwright E2E output (formal suite — tests/e2e/ IS committed, its run
# artifacts are not)
/test-results
/playwright-report
/blob-report
/playwright/.cache
